# TODO - Corrigir build no Vercel

- [x] Alinhar `vercel.json` com o projeto (usei `npm run build` e `npm ci`, ajustei `outputDirectory`).
- [ ] Confirmar qual pasta é gerada no build local (`dist/` vs `.output/`).
- [ ] Ajustar `outputDirectory` no `vercel.json` para a pasta real.
- [ ] Re-tentar deploy no Vercel e validar.

